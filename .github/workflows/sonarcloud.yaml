name: SonarCloud

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  analyse:
    uses: project-origin/.github/.github/workflows/reusable-sonarcloud.yaml@1c2d92ef17899775d5a5a7e9e91025e1d988049f
    secrets:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
