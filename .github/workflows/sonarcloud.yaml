name: SonarCloud

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  analyse:
    uses: project-origin/.github/.github/workflows/reusable-sonarcloud.yaml@749b106eed006da110a5d1c97b28c63a05ce96c9
    secrets:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
