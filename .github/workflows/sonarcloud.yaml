name: SonarCloud

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  analyse:
    uses: project-origin/.github/.github/workflows/reusable-sonarcloud.yaml@ea1e6b55f91bb9905d0e7dc98e61e86fb4373a47
    secrets:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
