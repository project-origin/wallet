name: SonarCloud

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  analyse:
    uses: project-origin/.github/.github/workflows/reusable-sonarcloud.yaml@d89b319332d8f0207f29f08cb4589e01f8eda06f
    secrets:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
