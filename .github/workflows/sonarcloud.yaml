name: SonarCloud

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  analyse:
    uses: project-origin/.github/.github/workflows/reusable-sonarcloud.yaml@c1e411bd412e1dca8aef08548b1dcdfb967dac38
    secrets:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
