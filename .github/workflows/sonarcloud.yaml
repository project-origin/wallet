name: SonarCloud

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  analyse:
    uses: project-origin/.github/.github/workflows/reusable-sonarcloud.yaml@5482a44c91bf6301c91205397d0d59667bbb8758
    secrets:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
