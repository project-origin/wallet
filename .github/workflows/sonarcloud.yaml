name: SonarCloud

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  analyse:
    uses: project-origin/.github/.github/workflows/reusable-sonarcloud.yaml@8f08922740bf507d9826f48b042ddc279018f517
    secrets:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
