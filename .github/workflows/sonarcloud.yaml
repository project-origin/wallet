name: SonarCloud

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  analyse:
    uses: project-origin/.github/.github/workflows/reusable-sonarcloud.yaml@9ba9919f878a26fd67f16d436ca69b4c69ee5947
    secrets:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
