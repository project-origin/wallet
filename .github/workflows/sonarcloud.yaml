name: SonarCloud

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  analyse:
    uses: project-origin/.github/.github/workflows/reusable-sonarcloud.yaml@09afbdbfc3a2fcde1e11e1864f7173f24312737c
    secrets:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
