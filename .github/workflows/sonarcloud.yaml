name: SonarCloud

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  analyse:
    uses: project-origin/.github/.github/workflows/reusable-sonarcloud.yaml@79ebfe86d111914219e66515cf6623015310def7
    secrets:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
