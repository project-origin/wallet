name: SonarCloud

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  analyse:
    uses: project-origin/.github/.github/workflows/reusable-sonarcloud.yaml@8f908927354af18bd9ac37f5cf166ce5d72ad155
    secrets:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
